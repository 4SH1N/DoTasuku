<%= current_user.username %>
<br><br><br><br>

<%= link_to "New task", new_task_path %>

<% @tasks.each do |task| %>
  <%= link_to task_path(task) do %>
    <div>
      <h2><%= task.title %></h2>
      <p><%= task.description %></p>
    </div>
  <% end %>
<% end %>
